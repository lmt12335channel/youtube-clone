"use client";

interface CommentRepliesProps {
  parentId: string;
  videoId: string;
}

export const CommentReplies = ({ parentId, videoId }: CommentRepliesProps) => {
  // Logic để fetch và hiển thị các replies sẽ được thêm vào sau
  return (
    <div className="pl-14 pt-4">
      {/* Tạm thời để trống */}
    </div>
  );
};